Proc(Formals formals, TypeExp typeExp, Exp exp)
Declare(Token var, TypeExp typeExp)
Formals(List<Token> varList, List<TypeExp> typeExpList)
AddPrim()
Exp(Token testExp, Token trueExp, Token falseExp)
SeqExps(List<Exp> expList)
SetExp(Token var, Exp exp)
Rands(List<Exp> expList)
VarExp(Token var)
ClassDecl(Ext ext, Statics statics, Fields fields, Methods methods)
ObjLoc(Exp exp)
SimpleLoc()
LetDecls(List<Token> varList, List<Exp> expList)